# GitHub Cleanup Checklist

These are manual GitHub UI steps. Do not delete old repos or rewrite history; route visitors around historical prototypes with clear labels.

## Profile

- Set profile bio to:

```text
Independent AI researcher building inspectable reasoning systems: constraint graphs, tension telemetry, verifier-backed traces.
```

- Set website to:

```text
https://www.boggersthefish.com/
```

- Ensure the profile README repo is public:

```text
BoggersTheFish/BoggersTheFish
```

## Pin Repos In This Order

1. `TS-Start-Here`
2. `TS-Reasoner-v0`
3. `TS-Codex-OS`
4. `TS-Core`
5. `TensionLM`
6. `BoggersTheCIG` or the strongest current CIG repo

## Repo Descriptions

Use sober descriptions that make the research ladder clear.

### TS-Start-Here

```text
Public map for the BoggersTheFish TS research ecosystem: what exists, what is toy-scope, and what comes next.
```

### TS-Reasoner-v0

```text
Inspectable toy reasoning system with constraint graphs, tension telemetry, verifier-backed traces, and release receipts.
```

### TS-Codex-OS

```text
Local-first project graph, tension ledger, planner, and release receipt substrate for Codex-driven development.
```

### TS-Core

```text
Lightweight graph dynamics kernel for TS-style propagation, relaxation, stability, and tension experiments.
```

### TensionLM

```text
TensionLM experiments: sigmoid pairwise tension attention as an inspectable alternative to softmax attention.
```

### BoggersTheCIG

```text
Provenance-aware claim/evidence graph for contradiction tracking, confidence updates, and inspectable knowledge state.
```

## Suggested Topics

Add these where relevant:

```text
ai, reasoning, interpretability, knowledge-graph, constraint-graph, verification, provenance, language-models, python, research
```

Use a narrower topic set for individual repos when possible. For example:

- TS-Reasoner-v0: `ai`, `reasoning`, `verification`, `constraint-graph`, `python`, `research`
- TS-Core: `graph`, `constraint-graph`, `simulation`, `python`, `research`
- TensionLM: `language-models`, `attention`, `interpretability`, `pytorch`, `ai`, `research`
- CIG repos: `knowledge-graph`, `provenance`, `contradiction`, `verification`, `python`

## Historical Repo Routing Note

Add this to older prototype READMEs over time:

```md
> Historical prototype.
> Current public research route: TS-Start-Here -> TS-Reasoner-v0 -> TS-Codex-OS -> TS-Core / TensionLM / CIG.
> Start here: https://github.com/BoggersTheFish/TS-Start-Here
```

## Do Not Pin For First Contact

- `GOAT-TS` until it has a sober historical README note.
- `BoggersTheMind`
- `BoggersTheAI` until the README is made sober.
- `woke-baby-llm`
- `BoggersTheLLM`
- `GOAT-OS`
- `BAGI`
- `BLM`
- `schizo_bet`
- `hehe`

## Cleanup Order

1. Publish the profile README repo.
2. Pin the six flagship repos.
3. Update descriptions and topics for the six pinned repos.
4. Add historical routing notes to older repos.
5. Archive or de-emphasize duplicate scratch branches only after the canonical route is clear.
