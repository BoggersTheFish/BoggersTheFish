# GitHub Manual Actions

These are UI-level actions that cannot be completed purely by editing markdown files.

## Pinned Repositories

Change pinned repositories to this order:

1. `TS-Start-Here`
2. `TS-Reasoner-v0`
3. `TS-Codex-OS`
4. `TS-Core`
5. `bozo`
6. `BoggersTheCIG`

Remove older first-contact pins such as:

- `GOAT-TS`
- `BoggersTheMind`
- `BoggersTheAI`
- `woke-baby-llm`
- `BoggersTheLLM`

## TS-Start-Here Metadata

Description:

```text
Public map for the BoggersTheFish TS research ecosystem: what exists, what is toy-scope, and what comes next.
```

Topics:

```text
ai, reasoning, interpretability, constraint-graph, verification, provenance, research
```

## Flagship Repo Descriptions

### TS-Reasoner-v0

```text
Inspectable toy reasoning system with constraint graphs, tension telemetry, verifier-backed traces, and release receipts.
```

### TS-Codex-OS

```text
Local-first project graph, tension ledger, planner, and release receipt substrate for Codex-driven development.
```

### TS-Core

```text
Lightweight graph dynamics kernel for TS-style propagation, relaxation, stability, and tension experiments.
```

### bozo

```text
TensionLM experiments: sigmoid pairwise tension attention as an inspectable alternative to softmax attention.
```

### BoggersTheCIG

```text
Local-first provenance-aware concept graph for confidence-weighted claims, contradiction tracking, and TS-style inspectable knowledge memory.
```

## TensionLM Naming

Consider either:

- renaming `bozo` to `TensionLM`, or
- creating a clean `TensionLM` mirror that points back to `bozo` as the original implementation history.

Do this later, after the profile route and README routing notes are stable.

## Historical Repos

Archive or de-emphasize historical repos only after README routing is in place. Do not delete history.

Use `docs/HISTORICAL_REPO_NOTE.md` as the reusable note.
